
  <div class="profile">
    <div class="profile-container profile-header-container">

      <div class="profile-header-infos">

      <% if current_user.profile.photo? %>
        <%  avatar_url = cl_image_path current_user.profile.photo.path %>
      <% elsif current_user.facebook_picture_url.present? %>
        <% avatar_url = image_path  current_user.facebook_picture_url  %>
        <% else %>
        <% avatar_url = "https://material-components-web.appspot.com/images/animal1.svg" %>
        <% end %>
         <%= image_tag avatar_url  %>

        <h1><%= @profile.first_name %> <%= @profile.last_name %></h1>


        <h2>Your Profile Description: <br> "<%= @profile.bio %>"</h2>



        <% if current_user == @profile.user %>
        <%= link_to edit_profile_path do %>
          <h2>Edit your profile<i class="fa fa-pencil-square-o" aria-hidden="true"></i></h2>
          <% end %>


      </ul>
    </div>


  <%= link_to "Show Your Bookings", profile_bookings_path(@profile, @bookings), class: "btn-normal"%>

  <%= link_to "Bookings for your places", owner_bookings_profile_bookings_path(@profile), class: "btn-normal" %>
<hr>




  <%= link_to "List Your Venue", new_venue_path, class: "btn-red"%>
  <% end %>
    </div>


  </div>
